import ThemeSelector from "../features/themes/ThemeSelector";

function Header() {
  return (
    <header>
      <p>Header</p>
      <ThemeSelector />
    </header>
  );
}

export default Header;
